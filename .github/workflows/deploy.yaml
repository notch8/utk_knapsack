name: "Deploy"
run-name: Deploy (${{ github.event.workflow_run.head_branch || github.ref_name }} -> ${{ inputs.environment || (github.event.workflow_run.head_branch == 'main' && 'dev') || (github.event.workflow_run.head_branch == 'staging' && 'staging') }}) by @${{ github.actor }}
on:
  workflow_run:
    workflows: ["Build Test Lint"]
    types: [completed]
    branches:
      - main
      - staging
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deploy to Environment'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging
      debug_enabled:
        type: boolean
        description: 'Run the build with tmate debugging enabled (https://github.com/marketplace/actions/debugging-with-tmate)'
        required: false
        default: false

jobs:
  determine-environment:
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push') }}
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.set.outputs.environment }}
    steps:
      - id: set
        name: Map branch/input to target environment
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "environment=${{ inputs.environment }}" >> "$GITHUB_OUTPUT"
          else
            case "${{ github.event.workflow_run.head_branch }}" in
              main) echo "environment=dev" >> "$GITHUB_OUTPUT" ;;
              staging) echo "environment=staging" >> "$GITHUB_OUTPUT" ;;
            esac
          fi

  deploy:
    needs: determine-environment
    uses: notch8/actions/.github/workflows/deploy.yaml@v1.0.11
    with:
      environment: ${{ needs.determine-environment.outputs.environment }}
    secrets: inherit
